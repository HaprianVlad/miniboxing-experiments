package Helpers

import scala.util.Random
import scala.collection.mutable.ArraySeq
import spire.random.immutable.Generator

/*object ArrayGenerator {

  private def generateArray[T](size:Int,array:ArraySeq[T],generator:Generator[T]):ArraySeq[T]={
			if(size == 0)
				array
			else generateArray(size-1,(generator.) +: array,generator)
	}
  
}*/